import { FilterChatsPipe } from './filter-chats.pipe';

describe('FilterChatsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChatsPipe();
    expect(pipe).toBeTruthy();
  });
});
